doctype html
html
 head
   title #{title}
  
 body
   header
         h1 #{title}
   div
     each costruzioni in lego
       div
         img(src='img1.jpg')
         h2
           | #{costruzioni.name} 
       div
         a(href='/')  | Guarda le istruzione